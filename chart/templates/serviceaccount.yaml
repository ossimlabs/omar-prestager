{{- if eq (include "omar-prestager.serviceAccount.enabled" $) "true" -}}
{{- $annotations := merge .Values.serviceAccount.annotations $.Values.global.serviceAccount.annotations -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "omar-prestager.serviceAccount.name" . }}
  labels:
    {{- include "omar-prestager.labels" . | nindent 4 }}
  {{- with $annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
